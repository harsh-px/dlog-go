dependencies:
  override:
    - mkdir -p "${GOPATH}/src/go.pedge.io/dlog"
    - rsync -azC --delete ./ "${GOPATH}/src/go.pedge.io/dlog/"
    - make -C "${GOPATH}/src/go.pedge.io/dlog" testdeps
test:
  override:
    - make -C "${GOPATH}/src/go.pedge.io/dlog" test
